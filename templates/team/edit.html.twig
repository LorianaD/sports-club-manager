{% extends 'base.html.twig' %}

{% block title %}Nouvelle équipe — Identité{% endblock %}

{% block header %}
<div class="player-create__wrapper">
    {% include "layout/header/_dashboard_page_topbar.html.twig" with {
        title: "Modifier l'équipe",
        backPath: path('team_index'),
        img: '✕'
    } %}
{% endblock %}
{% block body %}
    <main class="player-create">

        {{ form_start(formTeam, { attr: { class: 'player-create__form' } }) }}
            <div class="player-create_card">
                <section>
                    <div class="player-create__field">
                        {{ form_label(formTeam.name, "Nom de l'équipe", { label_attr: { class: 'form-body_label' } }) }}
                        {{ form_widget(formTeam.name, { attr: { class: 'form-body_input' } }) }}
                    </div>
                    <div class="player-create__field">
                        {{ form_label(formTeam.category, "Categorie", { label_attr: { class: 'form-body_label' } }) }}
                        {{ form_widget(formTeam.category, { attr: { class: 'form-body_input' } }) }}
                    </div>
                </section>
                <div class="player-create_actions">
                    <a href="{{ path('team_index') }}" class="btn btn-secondary">
                        Annulé
                    </a>
                    <button type="submit" class="btn btn-primary">
                        Validé
                    </a>
                </div>
            </div>
        {{ form_end(formTeam) }}

    </main>
</div>
{% endblock %}
{% block footer %}
    {% include "layout/dashboardFooter.html.twig" %}
{% endblock %}